{{#if sent}}
  <p>Enlace de recuperacion enviado</p>
{{else}}
  <h2>Luego de hacer clic en "Recuperar", se le enviará un link para restaurar
    su contraseña.</h2>
  <p>Si no ve el correo en su bandeja de entrada, revise la sección de spam</p>
  <form action="/api/auth/recovery" method="post">
    <label for="email">Ingrese su email</label>
    <input type="email" id="email" name="email" />
    <input type="submit" value="Recuperar" />
  </form>
{{/if}}